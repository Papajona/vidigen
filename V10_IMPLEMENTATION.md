# Vidigen V10

- Real local faster-whisper captions with segment and word timestamps.
- Cached Whisper model to avoid reloading on every request.
- Caption and provider health endpoints.
- Server-side Replicate adapter.
- Seedance and Runway provider slots with secrets kept server-side.
- Supabase/R2 data architecture retained.

Next: Supabase JWT verification, R2 signed media URLs, persistent generation workers/webhooks, and non-destructive word-level caption editing on the timeline.
